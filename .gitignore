*.key
*.pem
*.crt
*.cert
!public-ca.pem
linktree/generator
cert.perm
email-signatures/
# Arduino sketch credentials
arduino_secrets.h

# Local Apache config - GitHub Pages ignores .htaccess anyway
.htaccess

# HTTrack mirror cache
test/
*.tmp

# Node deps - only ever installed ad hoc for linting, never committed
node_modules/
package-lock.json

# Claude Code local settings/config (may contain API keys, machine-specific paths)
.claude/
.env
.env.*
!.env.example
